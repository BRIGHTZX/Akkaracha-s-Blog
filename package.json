{
  "name": "react-blog",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run build-backend && npm run build-client",
    "build-backend": "cd backend && npm install",
    "build-client": "echo 'Current directory: $(pwd)' && ls -l && cd client && npm install && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
